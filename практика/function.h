#pragma once
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int isInputCorrect();
vector<int> makeArr(vector<int>&, int);
vector<int> new_massiv(vector<int>&, vector<int>&, vector<int>&, vector<int>&, int);
void print_array(vector<int>&);
